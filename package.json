{
  "name": "MiWiFI-Mesh-Configuror",
  "version": "1.0.0",
  "description": "Change the config which xiaomi doesn't allow you to change.",
  "main": "index.js",
  "scripts": {
    "start": "electron  .",
	"pack": "electron-packager . MiWiFIMeshConfiguror --win --out ./myClient --arch=x64 --app-version=1.0.0 --electron-version=6.0.1"
  },
  "author": "Runc",
  "license": "GPL-2.0",
  "devDependencies": {
    "electron": "^9.4.0"
  },
  "dependencies": {
    "jquery": "^3.5.0"
  }
}
